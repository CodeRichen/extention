## 內含3個不同的擴充功能
1. HighlightSelectedWord(vscode的擴充功能):選取了某個單字後，檔案中的其他位置的相同單字，都用螢光筆註記
2. youtube-search-extension(Google的擴充功能):在YouTube用enter按下的方式搜尋的話，會在搜尋的尾巴加入"中文"，是用來查詢是否有中文翻譯
3. word-extension(Google的擴充功能):能夠更改背景(需要自備圖片)，但如果不是AI對話的網頁則不一定會成功
